import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // Reemplaza con el nombre de tu repositorio
    base: '/Astro-Hola-Mundo/',
    output: 'static',
});
